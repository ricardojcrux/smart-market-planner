export function usePrediction() {
  // Lógica placeholder para predicción
  const predictNeeds = (history: any[]) => {
    console.log("Generando predicción basada en historial...", history);
    return [];
  };

  return {
    predictNeeds
  };
}
